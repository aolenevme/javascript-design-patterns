/**
 * Allow an object to alter its behavior when its internal state changes.
 * The object will appear to change its class.
 *
 * In JS world: State is very close to the Strategy. They even have the same UML
 * diagrams. It achieves the same goal using another mechanism. Instead of
 * providing strategy explicitly, it depends on some state. From JS perspective
 * it can be implemented the same way as strategy pattern.
 */
